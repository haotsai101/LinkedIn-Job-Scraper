# Python related files/folders
__pycache__/
*.pyc
.pytest_cache/
/.venv/
.DS_Store

# Packaging / editable-install artifacts
*.egg-info/
build/
dist/
linkedin-job-scraper/

# Prevent accidental upload of login info
/logins.csv

# LLM credentials and user profile (sensitive)
/.env
/user_profile.json

# SQLite databases, backups and alternate versions
# e.g. linkedin_jobs.db, linkedin_jobs.db.bak, linkedin_jobs.db-wal, linkedin_jobs_uk.db
# (scoped to .db* so source files like linkedin_jobs.py are NOT ignored)
linkedin_jobs.db*
/linkedin_jobs_*.db
jobs.db

/csv_files/
R-project/
.Rproj.user
*.html
# Exploratory analysis notebooks/HTML live outside the repo (see README
# "Analysis notebooks"); keep ignored in case the dir is recreated locally.
analysis/
.vscode/
.tmp_*
.dagster_home/

# Generated / runtime output
logs/
application_log.json
created_accounts.json
llm_debug.jsonl*
debug_screenshots/
graphify-out/
media/

# Claude Code local settings (may contain machine-specific paths)
.claude/settings.local.json
.claude/agent-memory/
